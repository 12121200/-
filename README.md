Redis (REmote DIctionary Server) — это высокопроизводительное хранилище структур данных в оперативной памяти. Используется как база данных, кэш, брокер сообщений и движок потоковой обработки. Относится к классу NoSQL баз данных.
